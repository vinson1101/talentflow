# blockchain_lead_batch_001

本批用于校准区块链业务负责人岗位的 shortlist / 建联决策。

## 当前状态
- 这是从 judge 现有 fixture 落盘出的正式 calibration batch。
- 目的不是扩样本，而是让 judge 读取 `calibration_set`，不再走代码内置 fixture。

## 重点观察
- 区块链 BD / 渠道合作岗位不应误路由到 `product_manager`
- `sales_director` 模板边界应保持稳定
